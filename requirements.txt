fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
# mcp==1.2.0 requires pydantic>=2.10; the previous exact pin (2.9.2) conflicts
# with it, so this is a floor instead of an exact pin.
pydantic>=2.10,<3
mcp==1.2.0
pytest==8.3.3
pytest-asyncio==0.24.0
